example how to access read pid value:

{ 
	"name": "(gdb) Attach",
	"type": "cppdbg",
	"request": "attach",
	"program": "${workspaceFolder}....",
	"processId": "${command:extensions.readPid}",
	"MIMode": "gdb",

}
		
		
